                @model IEnumerable<MyMvcNetCore.ViewModels.Categories.ShowCategory>
@{
    Layout = null;
}
<div id="category-children-table">
    @if (Model.Any())
    {


        @* <div class="card shadow-sm mt-4">
            <div class="card-body">
                <h5 class="card-title">Subcategory List</h5>
                <div class="table-responsive"> *@
                    <table class="table table-striped table-hover">
                        <thead class="table-dark">
                        <tr>
                            <th>@Html.DisplayNameFor(model => model.Title)</th>
                            <th></th>
                            </tr>
                        </thead>
                        <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Title)
                                </td>
                                <td class="d-flex justify-content-end text-nowrap gap-1 ">
                                    <a class="btn btn-warning" asp-action="Edit" asp-route-id="@item.Id"><i class="bi bi-pencil-square"></i>&nbsp;Edit</a>
                                   
                                    <form asp-action="Delete" asp-route-id="@item.Id" class="d-inline" method="post">
                                        <button class="btn btn-danger" @(item.CanRemove ? string.Empty : "disabled")><i class="bi bi-trash"></i>&nbsp;Delete</button>
                                    </form>
                                </td>
                            </tr>
                        }
                        </tbody>
                    </table>
    @*             </div>
            </div>
        </div>  *@

       }
    else
    {
        <h3>Without Subcategory</h3>
    }
</div>